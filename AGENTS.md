# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

# Package Manager Policy

Do not use `npx` or `npm`. Company policy prohibits them for security reasons. Use `pnpx` for package execution and `pnpm` for package management instead.
